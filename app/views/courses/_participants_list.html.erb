<p>参加登録フォーム</p>
<%= form_for(@participant) do |f| %>


  <div class="field">
    <%= f.hidden_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>
